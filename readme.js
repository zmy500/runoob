/*
   第一步，用命令git add 告诉git,把文件放到git 仓库
   git add readme.js 

   第二步，用命令git commit 告诉git,把文件提交到仓库
   git commit -m "wrote a readme file"

   小结：
        初始化一个git 仓库 git  init
        添加文件到 git 仓库，分2步

        添加文件到git 仓库，分为两步
        1.git add <file>,注意，可反复多次使用，添加多个文件
        2.使用命令 git commit -m <message>,完成

*/

export default  {
    lal :'333'
}